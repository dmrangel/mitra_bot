# Copie este arquivo para .env e preencha com os valores reais.
# O .env NUNCA deve ir para o Git (já está no .gitignore).
#
# Como descobrir os IDs: com o bot rodando, envie !idgrupo dentro do grupo
# desejado a partir de um número que esteja em NUMEROS_VERIFICACAO.
# Formatos: pessoa = 5521999999999@c.us (ou 000000000000000@lid)
#           grupo  = 120363000000000000@g.us

# --- Destinos (obrigatórios) ------------------------------------------------

# Grupo(s) onde os pedidos de oração APROVADOS são publicados.
GRUPOS_ORACAO=

# Números que recebem os pedidos para aprovar e podem usar !pedidos / !aprovar.
# Separe por vírgula.
NUMEROS_VERIFICACAO=

# Grupo(s) avisados quando alguém pede atendimento humano (opção 8).
# Se ficar vazio, o aviso vai para NUMEROS_VERIFICACAO.
GRUPOS_NOTIFICACAO_HUMANO=

# Números que o bot deve ignorar completamente. Separe por vírgula.
BLACKLIST=

# --- Navegador / sessão -----------------------------------------------------

# Identificador da sessão. DEIXE VAZIO para continuar usando a pasta
# .wwebjs_auth/session que já existe no servidor. Preencher cria uma sessão
# nova (.wwebjs_auth/session-<SESSION_ID>) e OBRIGA A LER O QR CODE DE NOVO.
SESSION_ID=

# Caminho do Chrome/Chromium.
#   Linux (servidor):  /usr/bin/chromium-browser
#   Windows:           C:\Program Files\Google\Chrome\Application\chrome.exe
# Deixe VAZIO para usar o Chromium que já vem com o puppeteer (recomendado).
CHROME_PATH=

# Fixa uma versão do WhatsApp Web (ex.: 2.3000.1234567). Deixe VAZIO para a
# biblioteca escolher sozinha. Só preencha se a conexão estiver quebrando.
WEB_VERSION=

# Mata TODOS os processos do Chrome antes de subir. Use "true" apenas no
# servidor dedicado — num PC comum isso fecha as abas do usuário.
LIMPAR_CHROME=false

# --- Modo teste -------------------------------------------------------------

# Com "true", o bot só responde ao NUMERO_TESTE. Ótimo para validar mudanças
# sem incomodar os fiéis.
MODO_TESTE=false
NUMERO_TESTE=

# --- Tempos (opcionais, valores padrão indicados) ---------------------------

# Minutos de silêncio até perguntar "Podemos ajudar com algo mais?"
TEMPO_AVISO_MIN=5
# Minutos após o aviso até encerrar o atendimento.
TEMPO_ENCERRAMENTO_MIN=5
# Segundos de espera entre respostas do menu para o mesmo contato.
COOLDOWN_SEGUNDOS=5
# Segundos mínimos entre dois pedidos de oração do mesmo contato.
INTERVALO_PEDIDO_SEGUNDOS=30
# Dias que um pedido não aprovado fica na fila antes de ser descartado.
VALIDADE_PEDIDO_DIAS=7
# Minutos até soltar quem entrou em "pedido de oração" e sumiu.
EXPIRA_ORACAO_MIN=30
# Horas até soltar quem pediu atendimento humano e nunca foi liberado com !auto.
EXPIRA_HUMANO_HORAS=6

# Telefones públicos usados na resposta automática a ligações.
TELEFONES_SECRETARIA=(21) 2253-7564,(21) 97908-2767
